/*Find maximum value of Sum( i*arr[i]) with only rotations on given array allowed*/
#include <bits/stdc++.h>

int arrRotate(int* a,int n){

    int key,i;
    key=a[n-1];

    for(i=0;i<n;i++){

    }

}
int maxSum(int a[],int n){

}
int main(){
	int arr[]={1,20,2,10};
	int n=sizeof(arr)/sizeof(int);
    maxSum(arr,n);

        return 0;
}
